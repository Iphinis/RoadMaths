import grid

grid.create_grid(
    int(input("Nombre de lignes/colonnes sur chaque quadrant : ")),
    input(f"Voulez-vous sauvegarder la grille ? (1 si oui et autre sinon) : ")  == "1",
    input(f"Voulez-vous afficher la grille ? (1 si oui et autre sinon) : ") == "1"
    )